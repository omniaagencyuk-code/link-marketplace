/* 0023_delivery_review.sql, flattened for the Supabase SQL editor. Generated by scripts/flatten-migration.mjs - do not edit this file, edit the migration and run the script again. One statement per line, no line comments, nothing to lose to a newline. The commentary lives in the migration itself. */
begin;
do $$ begin create type public.item_approval as enum ('pending', 'approved', 'issue-raised'); exception when duplicate_object then null; end; $$;
alter table public.order_items add column if not exists approval public.item_approval not null default 'pending';
alter table public.order_items add column if not exists delivered_at timestamptz;
alter table public.order_items add column if not exists approved_at timestamptz;
alter table public.order_items add column if not exists auto_approve_at timestamptz;
alter table public.order_items add column if not exists auto_approved boolean not null default false;
comment on column public.order_items.approval is 'What the customer made of the delivery. Theirs to set, not ours.';
comment on column public.order_items.auto_approved is 'Approved by the clock rather than by the customer. Not the same as consent.';
create index if not exists order_items_awaiting_approval_idx on public.order_items (auto_approve_at) where approval = 'pending' and delivered_at is not null;
alter table public.orders add column if not exists completed_at timestamptz;
comment on column public.orders.completed_at is 'Set once every item on the order is approved. Rendered as "Complete".';
create table if not exists public.order_item_issues ( id uuid primary key default gen_random_uuid(), order_item_id uuid not null references public.order_items (id) on delete cascade, raised_by uuid references public.profiles (id) on delete set null, message text not null check (length(trim(message)) > 0), created_at timestamptz not null default timezone('utc', now()), resolved_at timestamptz, resolved_by text, resolution_note text );
create index if not exists order_item_issues_item_idx on public.order_item_issues (order_item_id, created_at desc);
create index if not exists order_item_issues_open_idx on public.order_item_issues (created_at desc) where resolved_at is null;
alter table public.order_item_issues enable row level security;
do $$ begin create policy "Customers read their own issues" on public.order_item_issues for select using ( exists ( select 1 from public.order_items join public.orders on orders.id = order_items.order_id where order_items.id = order_item_issues.order_item_id and orders.user_id = auth.uid() ) ); exception when duplicate_object then null; end; $$;
do $$ begin create policy "Customers raise issues on their own order" on public.order_item_issues for insert with check ( raised_by = auth.uid() and exists ( select 1 from public.order_items join public.orders on orders.id = order_items.order_id where order_items.id = order_item_issues.order_item_id and orders.user_id = auth.uid() ) ); exception when duplicate_object then null; end; $$;
do $$ begin create policy "Admins manage issues" on public.order_item_issues for all using (public.is_admin()) with check (public.is_admin()); exception when duplicate_object then null; end; $$;
alter table public.settings add column if not exists delivery_auto_approve_days smallint not null default 14 check (delivery_auto_approve_days > 0);
alter table public.settings add column if not exists post_approval_issue_days smallint not null default 30 check (post_approval_issue_days >= 0);
commit;
